你好！这是我们智链 AI (ClipFlow) 项目的最新的《架构级共识文档》以及底层的 API Schema 契约。

目前，我们面向 B 端的桌面渲染引擎（FastAPI + FFmpeg）已经 v1.0 封板。它支持纯异步排队渲染、X/Y轴物理隔离、单语种极速测试，并会通过 Webhook 异步发送完成通知。

我们的目标市场分为两块：
1. 实体出海下沉市场（东南亚/中东汽修、建材实体店）
2. 数字内容买量大军（广州出海游戏、互动影游、短剧 UA 团队）

现在，你的身份是【C 端微服务架构师】。
我们要新开一个独立的仓库，使用 Node.js + Telegraf 框架，开发一个极其轻量的 Telegram Bot，作为我们 PLG (产品驱动增长) 漏斗的移动端敏捷触角。

它的核心业务闭环是：
1. 接收用户的游戏实机录屏或车间实拍素材及指令。
2. 组装参数调用本地 FastAPI 引擎的提交接口。
3. 接收后端引擎发来的 Webhook 出片推送。
4. 将最终生成的视频发回给 Telegram 用户，并在 Telegram 内完成原生转发分享。
5. 建立配额墙（Paywall）：当用户达到免费生成额度时，引导其“升级获取企业专属桌面端营销主机”。

请仔细阅读我提供的文档，确认你完全理解了我们的物理隔离原则、异步 Webhook 回调契约以及 PLG 商业边界。
确认无误后，请直接给我出具这个 Telegram Bot 微服务仓库的【目录结构树】和基于 Node.js 的 【package.json 初始化技术选型指令】！