# DopaMatrix — Python 依赖清单
# 安装命令：pip install -r requirements.txt

# ── 异步 Web 框架 ──────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ── 数据库 ORM ────────────────────────────────────────────────────
sqlalchemy>=2.0.0

# ── 数据验证 ──────────────────────────────────────────────────────
pydantic>=2.0.0

# ── 环境变量 ──────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── LLM / TTS ─────────────────────────────────────────────────────
openai>=1.30.0
edge-tts>=6.1.9

# ── 指纹哈希（Phase 5.3 指纹注册机制）────────────────────────────
imagehash>=4.3.1       # 感知哈希 (pHash)

# ── 工具 ──────────────────────────────────────────────────────────
httpx>=0.27.0          # 异步 HTTP 客户端（供 StockProvider 等使用）
requests>=2.32.0       # 同步 HTTP 客户端（PexelsProvider / asset_provider.py）
tenacity>=9.0.0        # 重试库，用于 Webhook 指数退避重试
pyngrok>=8.1.2         # 本地开发时使用，将本地服务暴露到公网

# ── 日志 ──────────────────────────────────────────────────────────
loguru==0.7.2          # 生产级结构化日志（滚动 + 保留策略）
appdirs==1.4.4         # 跨平台用户日志目录定位

# ── 模板引擎 ──────────────────────────────────────────────────────
Jinja2>=3.1.6          # 模板引擎，用于生成 HTML 页面

# ── 构建 ──────────────────────────────────────────────────────────
pyinstaller>=6.21.0 # 将 Python 程序打包为可执行文件