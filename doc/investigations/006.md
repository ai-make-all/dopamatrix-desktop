提问：战友，你好！我们准备开始迭代，这里有个事项要确认一下，指令 0到指令10，哪些指令要用GPT-5.5执行，哪些要用Sonnet执行？

回答：战友，建议这样分工：

> **GPT-5.5 负责高风险架构、并发、数据库、主链路接入。**
> **Sonnet 负责低风险实现、测试补齐、UI 小改、报告整理。**

最重要原则：

> **不要让 GPT-5.5 和 Sonnet 同时改同一批文件。**
> 一个步骤一个模型执行，执行完提交报告，再决定下一步。

---

## 指令 0–10 模型分配建议

| 指令    | 内容                     | 推荐模型                         | 原因                            |
| ----- | ---------------------- | ---------------------------- | ----------------------------- |
| 指令 0  | 创建分支、检查工作区             | **Sonnet**                   | 低风险，偏流程检查                     |
| 指令 1  | 只读审查 Matrix 批量生成链路     | **GPT-5.5**                  | 需要理解全链路、定位重复根因                |
| 指令 2  | 1.5 最小设计方案             | **GPT-5.5**                  | 架构设计，必须稳                      |
| 指令 3  | SQLite migration 设计与实现 | **GPT-5.5**                  | 涉及数据结构、兼容、迁移风险                |
| 指令 4  | hash / signature 工具函数  | **Sonnet**                   | 局部实现，边界清晰                     |
| 指令 5  | Batch Diversity Ledger | **GPT-5.5**                  | 高风险，涉及事务、并发、状态机               |
| 指令 6  | 轻量 Variant Planner     | **GPT-5.5**                  | 核心算法逻辑，需要规划能力                 |
| 指令 7  | 接入现有批量生成流程             | **GPT-5.5**                  | 最高风险，涉及主链路改造                  |
| 指令 8  | 候选不足结构化返回              | **Sonnet**                   | 后端响应结构，局部改造                   |
| 指令 9  | 集成测试 / 回归测试            | **Sonnet 优先，GPT-5.5 复核失败用例** | Sonnet 写测试快，复杂失败再让 GPT-5.5 判断 |
| 指令 10 | 最终升级报告                 | **Sonnet 初稿，GPT-5.5 可复核**    | 报告整理 Sonnet 足够，架构准确性可再复核      |

---

## 更稳的执行策略

### 第一阶段：只读和设计

```text
指令 0：Sonnet
指令 1：GPT-5.5
把指令 1 报告贴给我
指令 2：GPT-5.5
把设计方案贴给我
```

这一步不要急着改代码。先让我看报告，确认接入点。

---

### 第二阶段：基础能力实现

```text
指令 3：GPT-5.5
指令 4：Sonnet
指令 5：GPT-5.5
```

这里重点是数据库、hash、ledger。
这三步做完后，平台就有 1.5 的基础设施了。

---

### 第三阶段：核心链路接入

```text
指令 6：GPT-5.5
指令 7：GPT-5.5
```

这两步最关键，尤其指令 7。
如果只选一个必须用 GPT-5.5 的步骤，就是指令 7。

---

### 第四阶段：体验和测试

```text
指令 8：Sonnet
指令 9：Sonnet
如果测试失败，把失败日志交给 GPT-5.5 分析
指令 10：Sonnet
```

---

## 哪些步骤必须用 GPT-5.5？

这几个不要交给 Sonnet 主导：

```text
指令 1：链路审查
指令 2：最小设计
指令 3：数据库迁移
指令 5：Batch Diversity Ledger
指令 6：Variant Planner
指令 7：接入主生成流程
```

尤其：

> **指令 5 和指令 7 是本次 1.5 最容易出问题的地方，必须用 GPT-5.5。**

---

## 哪些步骤适合 Sonnet？

```text
指令 0：准备工作
指令 4：工具函数
指令 8：结构化返回
指令 9：测试补齐
指令 10：报告整理
```

Sonnet 很适合做这些“边界清楚、执行型”的任务。

---

## 最推荐的组合

战友，我建议你按这个执行：

```text
0 Sonnet
1 GPT-5.5
反馈给我
2 GPT-5.5
反馈给我
3 GPT-5.5
4 Sonnet
5 GPT-5.5
反馈给我
6 GPT-5.5
7 GPT-5.5
反馈给我
8 Sonnet
9 Sonnet
10 Sonnet
```

一句话总结：

> **让 GPT-5.5 管“方向、架构、主链路、并发和数据库”，让 Sonnet 管“局部实现、测试、响应结构和文档”。**
